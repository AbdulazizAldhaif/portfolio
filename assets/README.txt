Drop your portrait photo here as  portrait.png  (square crop, at least 168x168 px).

The design project's copy could not be downloaded intact (the design API caps file
reads at 256 KB and the upload is larger), so the contact-section portrait is
hidden automatically until this file exists. No code changes needed — just add
the file and reload.
